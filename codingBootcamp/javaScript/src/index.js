// Functions
let name = "Kistune";
let count = 1;

const showAlert = () => {
  console.log(`${name}, wake up! This is alarm number ${count}!`);
  count += 1;
};
showAlert();
showAlert();
showAlert();
showAlert();
